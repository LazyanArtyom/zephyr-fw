# MCUboot child-image defaults.
#
# Keep production signing keys outside the repository. Pass the key file from a
# controlled build environment when production signing is enabled.

CONFIG_BOOT_SIGNATURE_TYPE_ECDSA_P256=y
