# Production configuration.
#
# This profile is intentionally quiet and avoids debug-oriented behavior.
# Enable shell or verbose logs explicitly only for controlled service builds.

CONFIG_LOG_DEFAULT_LEVEL=1
CONFIG_ASSERT=n
CONFIG_DEBUG=n
CONFIG_THREAD_MONITOR=n
CONFIG_INIT_STACKS=n

CONFIG_APP_HEARTBEAT_SERVICE=n
